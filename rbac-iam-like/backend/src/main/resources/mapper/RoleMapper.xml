<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aitech.rbac.mapper.RoleMapper">
    
    <!-- Find All Roles -->
    <select id="findAll" resultType="com.aitech.rbac.model.Role">
        SELECT * FROM roles
        <where>
            <if test="search != null and search != ''">
                (LOWER(role_name) LIKE LOWER(CONCAT('%', #{search}, '%')))
            </if>
        </where>
    </select>

    <!-- Find Role by ID -->
    <select id="findById" resultType="com.aitech.rbac.model.Role">
        SELECT * FROM roles WHERE role_id = #{id}
    </select>

    <!-- Find Roles by User ID -->
    <select id="findByUserId" resultType="com.aitech.rbac.model.Role">
        SELECT r.*
        FROM roles r
        INNER JOIN user_roles ur ON r.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>

    <!-- Insert Role -->
    <insert id="insert" parameterType="com.aitech.rbac.model.Role">
        INSERT INTO roles(role_id, role_name, description, is_system_role)
        VALUES(#{roleId}, #{roleName}, #{description}, #{systemRole})
    </insert>

    <!-- Update Role -->
    <update id="update" parameterType="com.aitech.rbac.model.Role">
        UPDATE roles
        SET role_name = #{roleName},
            description = #{description},
            is_system_role = #{systemRole}
        WHERE role_id = #{roleId}
    </update>

    <!-- Delete Role -->
    <delete id="delete">
        DELETE FROM roles WHERE role_id = #{id}
    </delete>

</mapper>
