<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aitech.rbac.mapper.UserAccessMapper">

    <select id="getUserAccess" resultType="com.aitech.rbac.dto.UserAccessFlatDTO">
        SELECT
            u.user_id,
            u.username,
            r.role_id,
            r.role_name,
            p.permission_id,
            p.permission_name,
            n.namespace_key,
            a.action_key
        FROM "users" u
        JOIN user_roles ur ON u.user_id = ur.user_id
        JOIN roles r ON ur.role_id = r.role_id
        JOIN role_permissions rp ON r.role_id = rp.role_id
        JOIN permissions p ON rp.permission_id = p.permission_id
        JOIN resource_access ra ON p.permission_id = ra.permission_id
        JOIN namespaces n ON ra.namespace_id = n.namespace_id
        JOIN action_types a ON ra.action_type_id = a.action_type_id
        WHERE u.user_id = #{userId}
    </select>

</mapper>
